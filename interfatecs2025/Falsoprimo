int main(){
    int num, i, cont;

    do{
        scanf("%d", &num);
    }while(num < 0 && num >= 1000000)

    for(i = 0; i < n; i++){
        if(num % i == 0) {
            cont++;
        if(cont > 3)
            break;
        }
    }
    if(cont == 3)
        printf("sim\n");
    else
        printf("nao\");n
}
